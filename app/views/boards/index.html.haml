%p#notice= notice
%h1 Boards
%table
  %thead
    %tr
      %th Name
      %th LINK
      %th{:colspan => "3"}
  %tbody
    - @boards.each do |board|
      %tr
        %td= board.name
        %td
          = link_to 'open', board_path(board)
%br
= link_to 'New Board', new_board_path
